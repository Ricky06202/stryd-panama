---
import Layout from '../layouts/Layout.astro'
import { GalleryPage } from '../components/GalleryPage'
---

<Layout title="Galería | Stryd Panama">
  <div class="relative pt-32 pb-20 overflow-hidden">
    <div
      class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full -z-10 opacity-30"
    >
      <div
        class="absolute top-0 left-1/4 w-96 h-96 bg-primary/20 rounded-full blur-[120px] animate-pulse"
      >
      </div>
      <div
        class="absolute bottom-0 right-1/4 w-96 h-96 bg-primary/10 rounded-full blur-[120px] animate-pulse delay-700"
      >
      </div>
    </div>

    <div class="max-w-7xl mx-auto px-4">
      <div class="text-center mb-16">
        <h1
          class="text-4xl md:text-6xl font-black mb-6 bg-clip-text text-transparent bg-gradient-to-r from-foreground to-foreground/70 uppercase"
        >
          Nuestra <span class="text-primary italic">Comunidad</span>
        </h1>
        <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
          Momentos, entrenos y la pasión que nos une en cada kilómetro.
        </p>
      </div>

      <GalleryPage client:load />
    </div>
  </div>
</Layout>
